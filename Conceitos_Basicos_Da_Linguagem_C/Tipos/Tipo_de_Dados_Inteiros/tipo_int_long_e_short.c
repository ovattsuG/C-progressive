#include <stdio.h>

int main() {
    printf("O tamanho de um inteiro em meu computador é %d bytes \n", sizeof(int));
    printf("int : %d bytes\n", sizeof( int ) );
    printf("short int: %d bytes\n", sizeof( short ) );
    printf("long int: %d bytes\n", sizeof( long ) );
}